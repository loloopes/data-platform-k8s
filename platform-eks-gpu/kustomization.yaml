apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Wraps platform-eks and adds GPU nodeSelector + nvidia.com/gpu requests.
# Selected by deploy-eks.sh when LLM_USE_GPU=true in k8s/.env.

namespace: data-platform

resources:
  - ../platform-eks

components:
  - ../components/llm-gpu
