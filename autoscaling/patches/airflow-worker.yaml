apiVersion: apps/v1
kind: Deployment
metadata:
  name: airflow-worker
spec:
  template:
    spec:
      containers:
        - name: airflow-worker
          resources:
            requests:
              cpu: 300m
              memory: 1Gi
            limits:
              memory: 4Gi
