apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-trino-mcp
spec:
  template:
    spec:
      containers:
        - name: mcp
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
