apiVersion: apps/v1
kind: Deployment
metadata:
  name: airflow-apiserver
spec:
  template:
    spec:
      containers:
        - name: airflow-apiserver
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              memory: 2Gi
